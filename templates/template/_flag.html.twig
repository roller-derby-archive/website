{% if noFrench ?? false and countryCode != 'FRA' %}
    <img class="rda-flag" alt="flag-{{ countryCode }}" src="{{ asset('images/flags/'~countryCode~'.webp') }}">
{% else %}
    <img class="rda-flag" alt="flag-{{ countryCode }}" src="{{ asset('images/flags/'~countryCode~'.webp') }}">
{% endif %}
