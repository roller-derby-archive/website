# Documentation on how to configure the bundle can be found at: https://symfony.com/doc/current/bundles/LiipImagineBundle/basic-usage.html
liip_imagine:
    driver: "gd"
    default_filter_set_settings:
        format: webp
    twig:
        mode: lazy
    filter_sets:
        logo_thumbnail:
            quality: 100
            filters:
                thumbnail: { size: [ 34, 34 ], mode: inset }
